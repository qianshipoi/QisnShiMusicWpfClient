<UserControl x:Class="QianShi.Music.Views.IndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QianShi.Music.Views"
             xmlns:uc="clr-namespace:QianShi.Music.Common.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <uc:ScrollViewerAnimation >
        <StackPanel VerticalAlignment="Stretch" MaxWidth="1280" Background="White">
            <TextBlock Margin="20,0" Text="by Apple Music" FontSize="28" FontWeight="Bold" />
            <ItemsControl ItemsSource="{Binding ApplyMusic}" Margin="0, 20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <uc:IsometricPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <uc:MusicCard Margin="20, 0" Cover="{Binding Cover}" Title="{Binding Title}" Description="{Binding Description}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DockPanel Margin="20, 0" HorizontalAlignment="Stretch" LastChildFill="True">
                <TextBlock DockPanel.Dock="Left" Text="推荐歌单" FontSize="28" FontWeight="Bold" />
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom"  Text="查看全部" Cursor="Hand" FontSize="13" FontWeight="Bold" Opacity=".65" />
            </DockPanel>
            <ItemsControl ItemsSource="{Binding RecommendPlayList}" Margin="0, 20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <uc:PlaylistCard Margin="20, 10" Cover="{Binding CoverImgUrl}" Playlist="{Binding}"
                                          Title="{Binding Name}" Description="{Binding Copywriter}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Margin="20,0" Text="For You" FontSize="28" FontWeight="Bold" />
            <UniformGrid Margin="20" Rows="1" Columns="2" MinHeight="200">
                <materialDesign:Card  Margin="0,0,10,0"
                      Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                      Padding="8"
                      UniformCornerRadius="6">
                    <materialDesign:Card.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="1">
                            <GradientStop Offset="0.0" Color="#9E9E9E" />
                            <GradientStop Offset="0.5" Color="White" />
                            <GradientStop Offset="1.0" Color="White" />
                        </LinearGradientBrush>
                    </materialDesign:Card.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock HorizontalAlignment="Stretch" Padding="10,0"
                            TextWrapping="Wrap"
                            Text="每日推荐" FontSize="66" FontWeight="Bold" />
                    </Grid>
                </materialDesign:Card>
                <materialDesign:Card Margin="10,0,0,0"
                      Padding="18"
                      UniformCornerRadius="6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MaxWidth="260" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <Border CornerRadius="10" BorderThickness="0"  
                            Height="{Binding RelativeSource={RelativeSource Self}, Path=ActualWidth}" Cursor="Hand"  Padding="16">
                            <Border.Background>
                                <ImageBrush ImageSource="https://oss.kuriyama.top/static/background.png" />
                            </Border.Background>
                        </Border>
                        <DockPanel Margin="20,0,0,0" Grid.Column="1" LastChildFill="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <TextBlock DockPanel.Dock="Top" FontWeight="Bold" FontSize="26" Text="坠落心空" />
                            <TextBlock DockPanel.Dock="Top" Text="小星星Aurora" FontSize="16" Opacity=".65" />
                            <Grid Height="60" VerticalAlignment="Bottom" >
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                                    <Button
                                          Style="{StaticResource MaterialDesignIconButton}"
                                          ToolTip="MaterialDesignIconButton">
                                        <materialDesign:PackIcon Kind="ThumbDown" Height="28"  Width="28" />
                                    </Button>
                                    <Button Margin="10, 0"
                                          Style="{StaticResource MaterialDesignIconButton}"
                                          ToolTip="MaterialDesignIconButton">
                                        <materialDesign:PackIcon Kind="Play" Height="40"  Width="40" />
                                    </Button>
                                    <Button
                                          Style="{StaticResource MaterialDesignIconButton}"
                                          ToolTip="MaterialDesignIconButton">
                                        <materialDesign:PackIcon Kind="SkipNext" Height="40"  Width="40" />
                                    </Button>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right"  Opacity=".65">
                                    <materialDesign:PackIcon Kind="Radio" Width="22" Height="22"  />
                                    <TextBlock Text="私人FM" FontWeight="Bold" FontSize="16" Margin="6,0"/>
                                </StackPanel>
                            </Grid>
                        </DockPanel>
                    </Grid>
                </materialDesign:Card>
            </UniformGrid>
            <TextBlock Margin="20,0" Text="推荐艺人" FontSize="28" FontWeight="Bold" />
            <ItemsControl Margin="0, 20" ItemsSource="{Binding RecommendSingerList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="20,0" HorizontalAlignment="Stretch">
                            <Ellipse  Height="{Binding RelativeSource={RelativeSource Self}, Path=ActualWidth}">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding Cover}" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Margin="0, 10, 0,0" Text="{Binding Name}" FontWeight="Bold" Opacity=".65" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DockPanel Margin="20, 0" HorizontalAlignment="Stretch" LastChildFill="True">
                <TextBlock DockPanel.Dock="Left" Text="新专速递" FontSize="28" FontWeight="Bold" />
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom"  Text="查看全部" Cursor="Hand" FontSize="13" FontWeight="Bold" Opacity=".65" />
            </DockPanel>
            <ItemsControl ItemsSource="{Binding NewAlbumList}" Margin="0, 20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <uc:PlaylistCard Margin="20, 10" Cover="{Binding CoverImgUrl}" Playlist="{Binding}"
                                             Title="{Binding Name}" Description="{Binding Artist.Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DockPanel Margin="20, 0" HorizontalAlignment="Stretch" LastChildFill="True">
                <TextBlock DockPanel.Dock="Left" Text="排行榜" FontSize="28" FontWeight="Bold" />
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom"  Text="查看全部" Cursor="Hand" FontSize="13" FontWeight="Bold" Opacity=".65" />
            </DockPanel>
            <ItemsControl ItemsSource="{Binding RankingList}" Margin="0, 20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <uc:PlaylistCard Margin="20, 10" Cover="{Binding CoverImgUrl}"  Playlist="{Binding}"
                                          Title="{Binding Name}" Description="{Binding UpdateFrequency}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </uc:ScrollViewerAnimation>
</UserControl>
