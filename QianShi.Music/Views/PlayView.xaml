<UserControl x:Class="QianShi.Music.Views.PlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QianShi.Music.Views"
             xmlns:uc="clr-namespace:QianShi.Music.Common.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Visibility="{Binding Display,Converter={StaticResource BooleanToVisibilityConverter}}"
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="SkyBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Width="388" Margin="0,-20,32,0" VerticalAlignment="Center"
                    HorizontalAlignment="Right">
            <Image Source="https://p2.music.126.net/PzVklWfH2G1gVuHd3a50Gg==/109951164984873532.jpg?param=1024y1024" />
            <Grid Margin="0,24,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="万有引力" FontWeight="Bold"  FontSize="22.4" />
                <ContentControl Grid.Row="1" FontSize="16" Opacity=".58">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="F*yy" />
                        <TextBlock Text="-" />
                        <TextBlock Text="万有引力" />
                    </StackPanel>
                </ContentControl>
                <materialDesign:PackIcon Grid.Column="1" Grid.RowSpan="2" Kind="CardsHeartOutline"
                                         HorizontalAlignment="Right" Background="SkyBlue"
                                         VerticalAlignment="Center" x:Name="LikeControl"
                                         Opacity=".58" Width="20" Height="20" />
                <DockPanel Margin="0,28,0,0" Grid.Row="2" Grid.ColumnSpan="2" LastChildFill="True">
                    <TextBlock x:Name="CurrentTimeControl" DockPanel.Dock="Left" Text="0:32" Opacity=".58" />
                    <TextBlock x:Name="TotalTimeControl" DockPanel.Dock="Right" Text="4:02"  Opacity=".58" />
                    <Slider x:Name="PlayProgressControl" Margin="8,0" />
                </DockPanel>
                <StackPanel Margin="0,18,0,0" Grid.Row="3" Grid.ColumnSpan="2" 
                            Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                            Width="30" Height="30">
                        <materialDesign:PackIcon Kind="RepeatVariant" Width="18" Height="18" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                            Margin="16,0,8,0" Width="38" Height="38">
                        <materialDesign:PackIcon Kind="SkipPrevious" Width="26" Height="26" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                            Width="48" Height="48" Click="Button_Click">
                        <materialDesign:PackIcon Kind="Pause" Width="36" Height="36" />
                        <!--<materialDesign:PackIcon Kind="Play" Width="28" Height="28" />-->
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconButton}" 
                            Margin="8,0,16,0" Width="38" Height="38" Cursor="Hand">
                        <materialDesign:PackIcon Kind="SkipNext" Width="26" Height="26" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Width="30" Height="30">
                        <materialDesign:PackIcon Kind="ShuffleVariant" Width="16" Height="16" />
                    </Button>
                </StackPanel>
                <MediaElement Name="me"  Visibility="Collapsed"/>
            </Grid>
        </StackPanel>
        <uc:LrcView x:Name="LrcView" Grid.RowSpan="2" Grid.Column="1" />
        <Rectangle HorizontalAlignment="Stretch" Grid.ColumnSpan="2" VerticalAlignment="Stretch" 
                   Fill="Transparent" MouseMove="PlayView_MouseMove" />
        <Button Grid.Column="1" Grid.Row="1" Style="{StaticResource MaterialDesignIconButton}" 
                ToolTip="MaterialDesignIconButton" Width="40" Height="40"
                VerticalAlignment="Top" HorizontalAlignment="Right"
                Margin="0,0,40,0" 
                Command="{Binding CloseCommand}">
            <materialDesign:PackIcon Kind="ChevronDown" FontWeight="Bold"  />
        </Button>
    </Grid>
</UserControl>
